@using eTrak.Dal.Models
@using Microsoft.AspNet.Identity
@using Microsoft.AspNet.Identity.EntityFramework
@using Microsoft.AspNet.Identity
@using System.Web.Mvc.Html

@if (Request.IsAuthenticated)
{
    <form id="idAllDetailsForm" novalidate method="post">
        <div>
            <!-- unsaved-warning-form-->
            <!-- Page Header -->
            @Html.Partial("../Partials/PartialSPAGlobals")
            <!-- End Page Header -->
            <!-- Page Header -->
            @Html.Partial("../Partials/PS_Page1PageHeader")
            <!-- End Page Header -->
        </div>
        @if (Request.IsAuthenticated && !User.IsInRole("Client User"))
        {
            <div class="row mb-20">
                <div class="col-md-5 col-lg-5">
                    @Html.Partial("../Partials/PV_MyEmails")
                </div><!-- End My Emails -->
            </div>
        }

        <input ng-show="true" type="hidden" id="idDataHasChanged" ng-model="dataHasChanged" />
        <input ng-show="true" type="hidden" id="idDTenCode" ng-model="DTEnquiry.enCode" />
        <input ng-show="true" type="hidden" id="idDTenCode" ng-model="DTEnquiry.enPrimaryContactName" />
        <div ui-view="enquiriesTab"></div>
    </form>

    <!--Google Analytics Script-->
    <script>
        (function (i, s, o, g, r, a, m) {
            i['GoogleAnalyticsObject'] = r; i[r] = i[r] || function () {
                (i[r].q = i[r].q || []).push(arguments)
            }, i[r].l = 1 * new Date(); a = s.createElement(o),
            m = s.getElementsByTagName(o)[0]; a.async = 1; a.src = g; m.parentNode.insertBefore(a, m)
        })(window, document, 'script', 'https://www.google-analytics.com/analytics.js', 'ga');

        ga('create', 'UA-47702928-1', 'auto');
        ga('send', 'pageview');
    </script>
    <!--Ended Google Analytics Script-->
}

